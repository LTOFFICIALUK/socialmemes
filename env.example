# App Configuration
# Your production URL (used for metadata and favicon resolution)
NEXT_PUBLIC_APP_URL=https://your-app-name.vercel.app

# Supabase Configuration
# Get these values from your Supabase project dashboard > Settings > API
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url_here
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Required for admin operations (bypasses RLS - keep secret!)
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# API Security
# Generate a secure random string for API authentication (used by GitHub Actions)
# You can generate one with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
API_SECRET_KEY=your_secure_api_key_here

# Solana Configuration
# RPC URL for Solana network (mainnet-beta, devnet, or custom RPC)
NEXT_PUBLIC_SOLANA_RPC_URL=https://api.mainnet-beta.solana.com

# Platform Wallet Configuration
# CRITICAL: Keep this private key SECRET! This is the platform treasury wallet.
# Never commit this to version control or share it publicly.
# This wallet is used to send payouts to users.
# Format: Base58 encoded private key (get this from your Phantom wallet export)
PLATFORM_WALLET_KEY=your_base58_encoded_private_key_here
